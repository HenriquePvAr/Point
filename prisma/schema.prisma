// prisma/schema.prisma

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Usuario {
  id                Int      @id @default(autoincrement())
  nome              String
  cpf               String   @unique
  email             String?  @unique
  senha             String
  cargo             String?
  status            String   @default("ativo")
  primeiroAcesso    Boolean  @default(true)
  tipo              String   @default("funcionario")
  codigoRecuperacao String?

  pontos       Ponto[]
  mensagens    Mensagem[]
  notificacoes Notificacao[]
  folgas       Folga[]
  consumos     Consumo[]   // <--- ADICIONADO: Relação com os consumos
}

model Ponto {
  id        Int      @id @default(autoincrement())
  data      DateTime @default(now())
  tipo      String   
  ip        String?
  usuario   Usuario  @relation(fields: [usuarioId], references: [id])
  usuarioId Int
}

model Mensagem {
  id        Int      @id @default(autoincrement())
  dataIso   String   
  texto     String
  usuario   Usuario  @relation(fields: [usuarioId], references: [id])
  usuarioId Int
}

// Tabela necessária para o "sininho" do Admin
model Notificacao {
  id        Int      @id @default(autoincrement())
  tipo      String   // Ex: "Entrada", "Saída"
  lida      Boolean  @default(false)
  criadoEm  DateTime @default(now())
   
  usuario   Usuario  @relation(fields: [usuarioId], references: [id])
  usuarioId Int
}

// Tabela de Folgas
model Folga {
  id        Int      @id @default(autoincrement())
  data      DateTime 
  usuario   Usuario  @relation(fields: [usuarioId], references: [id], onDelete: Cascade)
  usuarioId Int

  @@unique([usuarioId, data]) // Evita folga duplicada no mesmo dia
}

// <--- NOVA TABELA DE CONSUMOS (Cantina/Loja)
model Consumo {
  id        Int      @id @default(autoincrement())
  nomeItem  String
  valor     Float
  imagemUrl String?  // Foto do produto (opcional)
  data      DateTime @default(now())
  
  usuario   Usuario  @relation(fields: [usuarioId], references: [id], onDelete: Cascade)
  usuarioId Int
}